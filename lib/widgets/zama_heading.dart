import 'package:flutter/material.dart';

class ZamaHeading extends StatelessWidget {
  const ZamaHeading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
